sc.defaultParallelism   # max(no of threads,2)
-> 8



dataRDD=sc.textFile(r".....................")

dataRDD.getNumPartitions()
->13 


newRDD=dataRDD.repartition(10)
-> 10 (but this is for newRDD, dataRDD will be same since rdd
is immutable)



